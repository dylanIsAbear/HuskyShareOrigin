## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url = jdbc:mysql://localhost:3306/hs_local?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username = root
spring.datasource.password = 20001018lyh1

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.connection.isolation = 4
spring.jpa.properties.hibernate.current_session_context_class = thread
spring.mvc.static-path-pattern=/static/**

spring.http.encoding.enabled=true
spring.http.encoding.charset=UTF-8

huskyshare.resource.path=/Users/dylan/Desktop/Code/HuskyShareOrigin/src/main/resources/
huskyshare.resource.runtime.dir=target/

server.address=127.0.0.1
server.port=8080